#include <stdio.h> //input/output
#include <stdlib.h> //loads EXIT_SUCCESS and other important things

int main() {
    printf("Is the language C an Object-Oriented Language, or a Procedural Language?\n"); //procedural
    printf("What Linux command displays file contents?\n"); //cat
    printf("Which git command uploads the local repository content to a remote repository?\n"); //git push 
    
    return EXIT_SUCCESS;
}